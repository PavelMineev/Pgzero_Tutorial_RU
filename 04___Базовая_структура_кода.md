# Базовая структура кода

Вот минимальный каркас игры - пустое игровое окно с указанными параметрами:

```python
import pgzrun

WIDTH = 400
HEIGHT = 600
TITLE = "Your Game Name"

pgzrun.go()
```

Вот расширенный каркас игры с функциями, которые PgZero самостоятельно добавит в игровой цикл или запустит при определенном событии:

```python
import pgzrun
from pgzero.builtins import *  # Работает и без этого импорта, но IDE будет подчеркивать кучу ошибок.

screen: pgzero.game.screen

WIDTH = 400
HEIGHT = 600
TITLE = "Your Game Name"


def on_mouse_down(pos, button):
    """
    Вызывается при нажатии на кнопку мыши
    pos: кортеж (x, y) с координатами курсора
    button: номер кнопки
    """
    pass


def on_mouse_up(pos, button):
    """
    Вызывается при отпускании кнопки мыши
    pos: кортеж (x, y) с координатами курсора
    button: номер кнопки
    """
    pass


def on_mouse_move(pos, rel, buttons):
    """
    Вызывается при отпускании кнопки мыши
    pos: кортеж (x, y) с координатами курсора в конце движения
    rel: кортеж (delta_x, delta_y) с величинами смещения курсора
    buttons: множество с номерами зажатых кнопок мыши
    """
    pass


def on_key_down(key, mod, unicode):
    """
    Вызывается при нажатии кнопки клавиатуры.
    key: номер нажатой клавиши
    mod: битовая маска с нажатиями клавиш-модификаторов
    unicode: печатаемый символ (если он есть у нажатой клавиши)
    
    ВАЖНО: эта функция обрабатывает только нажатие кнопки.
    Повторно срабатывает, только если клавишу отпустить и снова нажать.
    Обработку удержания клавиш нужно производить в функции update.
    """
    pass


def on_key_up(key, mod):
    """
    Вызывается при нажатии кнопки клавиатуры
    key: номер нажатой клавиши
    mod: битовая маска с нажатиями клавиш-модификаторов
    """
    pass


def on_music_end():
    """
    Вызывается при завершении музыкальной дорожки, 
    если ее воспроизведение не зациклено
    """
    pass


def update(dt):
    """
    Вызывается в игровом цикле (по умолчанию - 60 раз в секунду). 
    Предназначена для цикличного запуска команд,
    связанных с вычислением и обновлением игровых данных.
    
    Параметр dt (dummy time) стоит указывать для того, 
    чтобы цикл игры работал не с произвольной скоростью,
    а строго по 60 итераций в секунду.
    """
    pass


def draw():
    """
    Вызывается с привязкой к функции update или 
    функциям событий клавиатуры и мыши.
    Предназначена для запуска команд, связанных с отрисовкой
    фона, геометрических примитивов, текста и игровых объектов.
    
    Примечание: даже если функция update не нужна, стоит создать ее,
    поместив внутрь команду pass. Это позволит обеспечить циклический
    запуск функции draw.
    """
    pass


pgzrun.go()
```

Подробнее о событиях написано здесь:\
https://pygame-zero.readthedocs.io/en/latest/hooks.html
